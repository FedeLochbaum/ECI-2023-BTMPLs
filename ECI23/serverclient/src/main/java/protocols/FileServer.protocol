typestate FileServer {

  Init = {
    boolean start(Socket): <true: Connected, false: Close>
  }

  Connected = {
    boolean hasRequest(): <true: Sending, false: Connected>
    void close(): end;
  }

  Sending = {
    void sendFile(): Connected
  }

  Close {
    void close(): end
  }
}
